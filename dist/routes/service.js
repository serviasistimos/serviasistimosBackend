"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = require("express");
const service_1 = require("../controllers/service");
const authentication_1 = require("../middlewares/authentication");
const serviceRoutes = express_1.Router();
const serviceController = new service_1.ServiceController();
serviceRoutes.post('/createService', [authentication_1.verifyToken], (req, res) => { serviceController.createService(req, res); });
serviceRoutes.put('/updateService/:id', authentication_1.verifyToken, (req, res) => { serviceController.updateService(req, res); });
serviceRoutes.delete('/deleteService/:id', authentication_1.verifyToken, (req, res) => { serviceController.deleteService(req, res); });
serviceRoutes.get('/services', authentication_1.verifyToken, (req, res) => { serviceController.getServices(req, res); });
serviceRoutes.get('/service/:id', authentication_1.verifyToken, (req, res) => { serviceController.getServiceById(req, res); });
exports.default = serviceRoutes;
